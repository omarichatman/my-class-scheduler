<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.omari.cis436proj4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/alarm_clock_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MathSubject"/>
        <activity android:name=".HistorySubject"/>
        <activity android:name=".ScienceSubject"/>
        <activity android:name=".CodingSubject"/>
        <activity android:name=".NotificationHelper"/>
    </application>

</manifest>